# Linux install (no Windows wheel paths). Use from project root:
#   python3 -m venv venv && source venv/bin/activate
#   pip install -r requirements-linux.txt
#   pip install -e qwen_tts_package/

fastapi>=0.135.0
uvicorn>=0.44.0
python-multipart>=0.0.26
numpy>=2.0.0
requests>=2.28.0
websockets>=12.0

# PyTorch (CUDA 12.8) — adjust index if your driver differs
--extra-index-url https://download.pytorch.org/whl/cu128
torch>=2.8.0
torchaudio>=2.8.0

# qwen-tts deps (also installed via editable qwen_tts_package/)
transformers==4.57.3
accelerate==1.12.0
librosa>=0.11.0
soundfile>=0.12.0
einops>=0.8.0
onnxruntime>=1.24.0
sox>=1.5.0
